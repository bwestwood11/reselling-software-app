# Copy this to .env at the root if using root-level env vars.
# Each app also has its own .env.example — copy those to .env.local inside each app.
#
# apps/api/.env.example  → apps/api/.env
# apps/web/.env.example  → apps/web/.env.local
